# examples/server_ws.js 环境变量
#
# 说明：
# - 该文件会通过 Node.js 的 --env-file 自动加载（见 examples/package.json）。
# - 所有值都是字符串；需要数字的配置会在运行时做 parseInt。
# - 可按需修改；若删除某项，则脚本会回退到内置默认值。
#
# CAP_ENCODER
# - 说明：选择后端图像编码器
# - 可选值：
#   - ffmpeg：使用 ffmpeg 进行 MJPEG 实时编码（更偏实时）
#   - sharp：使用 sharp 编码 JPEG（兼容但通常更吃 CPU）
# - 默认值（脚本内）：
#   - Windows：ffmpeg
#   - macOS/Linux：sharp
CAP_ENCODER=ffmpeg

# CAP_FFMPEG_PATH
# - 说明：手动指定 ffmpeg 可执行文件路径（优先级最高），例如：
#   - Windows：C:\path\to\ffmpeg.exe
#   - macOS/Linux：/usr/local/bin/ffmpeg
# - 留空表示使用 @ffmpeg-installer/ffmpeg 提供的内置二进制（若可用），否则退回到 PATH 中的 ffmpeg
# - 可选值：任意可执行路径
CAP_FFMPEG_PATH=

# CAP_FPS
# - 说明：屏幕采集帧率上限（过高会增加 CPU/带宽压力）
# - 可选值：1..240（建议 30/60）
# - 默认值（脚本内）：60
CAP_FPS=60

# CAP_MAX_WIDTH
# - 说明：编码前下采样的最大宽度（等比缩放）；0 表示不缩放
# - 可选值：
#   - 0：不缩放
#   - 正整数：例如 1920/1280/960/720
# - 默认值（脚本内）：
#   - Windows：1280
#   - macOS/Linux：0
CAP_MAX_WIDTH=0

# CAP_JPEG_QUALITY
# - 说明：JPEG 质量（越高越清晰但越大/越慢）
# - 可选值：1..100（建议 35..70）
# - 默认值（脚本内）：60
CAP_JPEG_QUALITY=60

